<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="BadgeView">
        <!-- 徽章背景色 -->
        <attr name="badge_bgColor" format="reference|color" />
        <!-- 徽章文本的颜色 -->
        <attr name="badge_textColor" format="reference|color" />
        <!-- 徽章文本字体大小 -->
        <attr name="badge_textSize" format="reference|dimension" />
        <!-- 徽章背景与宿主控件上下边缘间距离 -->
        <attr name="badge_verticalMargin" format="reference|dimension" />
        <!-- 徽章背景与宿主控件左右边缘间距离 -->
        <attr name="badge_horizontalMargin" format="reference|dimension" />
        <!-- 徽章文本边缘与徽章背景边缘间的距离 -->
        <attr name="badge_padding" format="reference|dimension" />
        <!-- 徽章图片 -->
        <attr name="badge_drawable" format="reference" />
        <!-- 徽章在宿主控件中的位置 -->
        <attr name="badge_gravity" format="enum">
            <enum name="leftTop" value="0" />
            <enum name="leftCenter" value="1" />
            <enum name="leftBottom" value="2" />
            <enum name="centerTop" value="3" />
            <enum name="center" value="4" />
            <enum name="centerBottom" value="5" />
            <enum name="rightTop" value="6" />
            <enum name="rightCenter" value="7" />
            <enum name="rightBottom" value="8" />
        </attr>
        <!-- 徽章描边宽度 -->
        <attr name="badge_borderWidth" format="reference|dimension" />
        <!-- 徽章描边颜色 -->
        <attr name="badge_borderColor" format="reference|color" />

        <attr name="badge_textMaxSize" format="dimension" />
        <attr name="badge_textMinSize" format="dimension" />
        <!-- 徽章的最大边 -->
        <attr name="badge_maxSize" format="dimension" />

        <attr name="badge_dotSize" format="dimension" />
    </declare-styleable>


</resources>